# Bluesky Account Packet Skill

1. Run registry validation and select the next candidate ordered by backlog count then agency ID.
2. Derive `<agency>-archive.bsky.social`, then `nzgov-<agency>-archive.bsky.social`.
3. Construct the Gmail alias only in local operator memory from `edithatogo+bluesky-<slug>@gmail.com`.
4. Record agency sources, profile copy, GitHub Environment, issue, and acceptance checks without credentials.
