# Bluesky Environment Handoff Skill

Create environment `bluesky-mirror-<agency-id>`. Set fixed environment secrets `BLUESKY_HANDLE` and `BLUESKY_APP_PASSWORD` without printing values. Run `Bluesky Mirror Preflight`; keep the account disabled when any check fails.
