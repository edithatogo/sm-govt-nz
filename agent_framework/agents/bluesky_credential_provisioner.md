# Bluesky Credential Provisioner

Transfer the newly generated app password directly into the matching GitHub Environment as `BLUESKY_APP_PASSWORD`, set `BLUESKY_HANDLE`, and retain the primary password only in Windows Credential Manager. Never echo or persist credentials.
