# Specification

Maintain one nonsecret registry row per distinct government public identity. Group multi-platform sources by canonical agency ID, preserve genuinely separate services, use deterministic handles and isolated environments, and prohibit credentials or complete registration aliases in committed files. Primary handles use `<organisation-abbreviation>-<country-or-jurisdiction>-arc.bsky.social`; collisions append a stable number without expanding the organisation name.
