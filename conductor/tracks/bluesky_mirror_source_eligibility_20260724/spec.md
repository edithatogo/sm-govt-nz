# Specification

Fail closed on mirror candidates using source ID, normalized platform, canonical URL, visibility, agency, and explicit account allowlists. Legacy records with missing provenance must not fall back to broad agency matching. Record rejection reasons in a machine-readable eligibility report.

Acceptance requires fixtures for missing source IDs, sibling platforms, retired URLs, canonical URL variants, and valid LinkedIn posts.
