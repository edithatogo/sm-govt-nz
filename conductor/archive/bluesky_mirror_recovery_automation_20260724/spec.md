# Specification

Provide a recovery command and workflow that inspect the pause reason, verify known URIs publicly, classify deleted/reconciled/pending states, record evidence, and clear pauses only when deterministic recovery conditions pass. Expected delayed indexing remains report-only; destructive cleanup remains separately authorized.
